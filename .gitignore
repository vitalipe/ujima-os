pad.md
/issues/
/tmp/
desktop/console/tmp/
/out/
runtime/config/*.local.edn
tools/config/*.local.edn

# personal per-network wifi wrappers (e.g. wifi.home); the `wifi` tool itself stays tracked
os/pipeline/dev/kit/wifi.*

# built in the image chroot or on a Pi, not vendored (desktop/{files,launcher}/CMakeLists.txt)
desktop/files/bin/
desktop/launcher/bin/
