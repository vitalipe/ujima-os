;; the Files app's runtime beyond the core Qt Quick set (the install stage — the launcher shares
;; it): the dashed ring of the Temporary card. The C++ host is built by the desktop stage.
{:apt ["qml6-module-qtquick-shapes"]}
