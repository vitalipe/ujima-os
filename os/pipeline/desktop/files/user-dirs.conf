# managed by the ujima image build — source: os/pipeline/desktop/files/user-dirs.conf
#
# xdg-user-dirs-update must never recreate Desktop/Documents/… in home: home IS the Places screen
enabled=False
